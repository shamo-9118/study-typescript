export const Primitive = () => {
  const bar: boolean = false;

  const voo: string = "今日は";
  const goo: string = `アンガー${voo}マネー`;

  const too: null = null;
  const un: undefined = undefined; //undefinedを使う。

  const roo: false = false;
  const tuu: true = true; //Boolean Literal Types
  return;
};
